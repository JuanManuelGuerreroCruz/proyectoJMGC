<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jmgc.itics.tesoem.edu.proyectomole">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MoleDeAjonjoliActivity"></activity>
        <activity android:name=".MoleRojo3Activity" />
        <activity android:name=".MoleVerdeActivity" />
        <activity android:name=".MolePoblanoActivity" />
        <activity android:name=".ManchaMantelesActivity" />
        <activity android:name=".MoleColoraditoActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MoleAlcaparradoActivity" />
        <activity android:name=".MoleAlmendradoActivity" />
        <activity android:name=".MoleAmarillooAmarillitoActivity" />
        <activity android:name=".MoleBlancoodeNoviaActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>